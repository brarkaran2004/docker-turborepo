{
  "name": "turbo-docker",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "generate:db":"cd packages/db &&  bunx prisma migrate dev &&bunx prisma generate",
    "start:fe":"cd apps/fe && bun run dev",
    "start:ws":"bun run generate:db &&  cd apps/ws && bun run --watch src/index.ts",
    "start:http":"bun run generate:db && cd apps/http && bun run --watch index.ts"

  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.7.4",
    "typescript": "5.9.2"
  },
  "dependencies": {
     "next": "16.1.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
